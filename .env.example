# Copy to .env and fill in. EXPO_PUBLIC_* values are inlined into the app
# bundle at build time — restart `expo start` after changing them.

EXPO_PUBLIC_BACKEND_URL=https://smahi1.pythonanywhere.com
EXPO_PUBLIC_CLOUDINARY_CLOUD_NAME=dvj6cw5dq

# Required for voice search (Whisper). Keep this out of git.
EXPO_PUBLIC_OPENAI_API_KEY=
